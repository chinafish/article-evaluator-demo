# 文章评估智能体 v1.2 实施指南

**版本**: v1.2 P0优化版
**发布日期**: 2026-03-04
**目标**: MVP验证 - 决策优先级判断 + 优化卡片呈现

---

## 📋 快速开始

### 1. 环境准备

```bash
# 创建项目目录
mkdir article-evaluator-demo
cd article-evaluator-demo

# 初始化项目
npm init -y

# 安装依赖
npm install express axios @mozilla/readability jsdom dotenv
npm install --save-dev nodemon
```

### 2. 文件结构

```
article-evaluator-demo/
├── src/
│   ├── prompts/
│   │   ├── saasPrompt.v1.2.js      # SaaS专业提示词 v1.2
│   │   └── generalPrompt.v1.2.js   # 通用提示词 v1.2
│   ├── services/
│   │   ├── industryRouter.js       # 路由模块
│   │   ├── saasAnalyzer.js         # SaaS分析器（需更新）
│   │   └── generalAnalyzer.js      # 通用分析器（需更新）
│   └── server.js                   # 服务器入口
├── public/
│   └── index.v1.2.html             # 前端页面 v1.2
├── .env                             # 环境变量配置
└── package.json
```

### 3. 环境变量配置

创建 `.env` 文件：

```env
# AI服务配置
AI_PROVIDER=openai  # 可选：openai、qwen、wenxin
AI_API_KEY=your_api_key_here
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-3.5-turbo

# 服务端口
PORT=3000
```

---

## 🚀 启动服务

### 开发模式

```bash
# 使用nodemon启动
npm run dev

# 或直接启动
node src/server.js
```

### 访问地址

```
http://localhost:3000
```

---

## 🔧 核心模块更新说明

### 更新点1：提示词升级（v1.1 → v1.2）

**主要变化**：
- ✅ 增加决策优先级判断（高/中/低）
- ✅ 增加相关性评估
- ✅ 增加紧迫性判断
- ✅ 输出JSON格式增加新字段

**文件位置**：
- `src/prompts/saasPrompt.v1.2.js`
- `src/prompts/generalPrompt.v1.2.js`

**JSON输出格式对比**：

```javascript
// v1.1 输出格式
{
  "judgment": "认同",
  "input_diagnosis": "...",
  "macro_benchmark": "...",
  "blind_spot_alert": "...",
  "strategic_action": "..."
}

// v1.2 输出格式
{
  "judgment": "认同",
  "relevance": "高",          // 🆕 相关性
  "urgency": "是",            // 🆕 紧迫性
  "urgency_reason": "...",    // 🆕 紧迫性理由
  "input_diagnosis": "...",
  "macro_benchmark": "...",
  "blind_spot_alert": "...",
  "strategic_actions": [      // 🆕 数组格式，包含优先级
    {
      "priority": "高",
      "action": "...",
      "timeline": "...",
      "reason": "..."
    }
  ],
  "summary": "..."            // 🆕 一句话总结
}
```

### 更新点2：前端卡片优化

**主要变化**：
- ✅ 核心信息区前置（认同度、相关性、紧迫性）
- ✅ 优先级视觉标识（🔴高/🟡中/🟢低）
- ✅ 可折叠区域（默认收起）
- ✅ 行动建议按优先级分组展示

**文件位置**：
- `public/index.v1.2.html`

**卡片结构**：

```
┌─────────────────────────────┐
│  【核心信息区】- 始终可见     │
│  ✅ 认同 | 相关性⭐⭐⭐⭐⭐   │
│  🔴 需要立即关注              │
│  💡 一句话总结                │
├─────────────────────────────┤
│  【输入诊断】    [展开 ▼]     │
│  【宏观基准】    [展开 ▼]     │
│  【关键盲区】    [展开 ▼]     │
├─────────────────────────────┤
│  【行动建议】                │
│  🔴 高优先级                 │
│     ☐ 具体行动1              │
│     ☐ 具体行动2              │
│  🟡 中优先级                 │
│     ☐ 具体行动3              │
│  🟢 低优先级                 │
│     ☐ 具体行动4              │
└─────────────────────────────┘
```

---

## 📝 测试验证方案

### 测试文章准备

准备5-10篇测试文章，覆盖以下类型：

| 类型 | 数量 | 说明 |
|------|------|------|
| **公司级分析** | 2-3篇 | 特定SaaS公司的分析报告 |
| **行业级分析** | 2-3篇 | SaaS行业趋势、市场格局 |
| **观点性文章** | 2-3篇 | PLG vs SLG等方法论观点 |

### 验收标准

| 维度 | 检查项 | 通过标准 |
|------|-------|---------|
| **功能完整性** | 输入输出 | ✅ 支持粘贴链接和内容<br>✅ 生成完整评估结果 |
| **优先级判断** | 决策支持 | ✅ 每条建议标注优先级<br>✅ 优先级判断合理<br>✅ 行动建议具体可执行 |
| **卡片呈现** | 用户体验 | ✅ 核心信息前置<br>✅ 可折叠区域正常<br>✅ 优先级视觉清晰 |
| **内容质量** | 评估准确性 | ✅ 事实判断准确<br>✅ 盲区指出有价值<br>✅ 建议可执行 |

### 验收流程

```
第1步：功能测试（3月5日-3月7日）
├─ 测试输入功能
├─ 测试评估生成
└─ 测试卡片展示

第2步：内容质量验证（3月8日-3月12日）
├─ 准备5-10篇测试文章
├─ 生成评估结果
└─ 人工校对质量

第3步：高管验收（3月13日-3月15日）
├─ 现场演示
├─ 高管亲自测试
└─ 收集反馈

第4步：迭代优化（3月16日-3月19日）
├─ 根据反馈优化提示词
├─ 调整卡片呈现
└─ 重新验证

第5步：MVP验收（3月20日）
└─ 最终验收
```

---

## 🐛 常见问题

### Q1: 提示词返回的JSON格式不正确

**解决方案**：
1. 在prompt中强调"必须输出有效的JSON格式"
2. 在解析前增加JSON格式验证
3. 提供兜底方案：如果JSON解析失败，提取第一个{}内容

**代码示例**：
```javascript
function parseAIResponse(response) {
  try {
    return JSON.parse(response);
  } catch (error) {
    // 尝试提取JSON部分
    const jsonMatch = response.match(/\{[\s\S]*\}/);
    if (jsonMatch) {
      try {
        return JSON.parse(jsonMatch[0]);
      } catch (e) {
        // 仍然失败，返回兜底对象
        return {
          judgment: '不明确',
          error: 'JSON解析失败'
        };
      }
    }
    throw new Error('无法解析AI响应');
  }
}
```

### Q2: 优先级判断不准确

**解决方案**：
1. 在提示词中增加优先级判断示例
2. 明确优先级判断标准
3. 在测试中收集反馈，调整提示词

### Q3: 前端卡片样式异常

**解决方案**：
1. 检查CSS是否正确加载
2. 检查JavaScript是否正确执行
3. 使用浏览器开发者工具调试

---

## 📊 性能优化建议

### 1. 响应时间优化

当前目标：≤10秒（95%请求）

**优化方向**：
- 使用更快的模型（如GPT-3.5-turbo）
- 减少提示词长度
- 缓存常见文章的评估结果

### 2. 成本优化

**优化方向**：
- 使用token计费更低的模型
- 批量处理多个评估请求
- 设置请求频率限制

---

## 🔄 版本对比

| 特性 | v1.1 | v1.2 |
|------|------|------|
| **路由策略** | LLM智能路由 | ✅ 保持 |
| **提示词版本** | v1.1 | ✅ v1.2 P0优化 |
| **优先级判断** | ❌ 无 | ✅ 有（高/中/低） |
| **相关性评估** | ❌ 无 | ✅ 有（高/中/低） |
| **紧迫性判断** | ❌ 无 | ✅ 有（是/否） |
| **行动建议格式** | 单段文本 | ✅ 结构化数组 |
| **卡片呈现** | 基础卡片 | ✅ 优化卡片（核心信息前置） |
| **可折叠区域** | ❌ 无 | ✅ 有 |

---

## 📈 下一步计划

### v1.3 预规划（基于MVP反馈）

**可能的优化方向**：
1. **方案一实现**：情境输入模块（用户填写公司信息）
2. **方案二实现**：团队对齐模式（生成讨论引导材料）
3. **对比评估功能**：支持多篇文章对比
4. **待办清单功能**：一键添加行动建议到待办
5. **历史记录**：保存评估历史

---

## 📞 联系方式

如有问题，请联系：
- 产品经理：[待填写]
- 技术负责人：[待填写]

---

**祝MVP验证顺利！** 🎉
